## Credentials are stored securely in Replit Secrets (BOT_TOKEN, MONGODB_URI).
## All other configuration is managed via Replit environment variables.
## See config.env.example for documentation on each setting.
