# Replit Agent Instructions

This is a **Python only** project. Do not generate:

- `package.json`
- `pnpm-lock.yaml`
- Any `node_modules` or JavaScript/TypeScript files
- Any web framework other than the minimal Flask keep‑alive described below.

## What you MUST generate

- All Python files needed for the Telegram bot as specified in `PROMPT.md`.
- A `RULES.md` file containing the AI agent rules (already provided).
- A `README.md` explaining the project, setup, and usage.
- A `pyproject.toml` or `requirements.txt` with the exact dependencies:
  - `python-telegram-bot==22.5`
  - `motor`
  - `flask`
  - `asyncio` (standard library, but list it if you want)
- A `.replit` file with:

run = "python -m tgbot_tcf"
language = "python3"

- **Keep‑alive**: The bot must start a Flask server on `0.0.0.0:8080` in a daemon thread before `run_polling()`. This is the ONLY web‑related code allowed.

## Important

- Read and follow `RULES.md` at all times.
- Read and implement every detail in `PROMPT.md`.
- No emoji anywhere.