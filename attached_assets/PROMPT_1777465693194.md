Build a production-ready Telegram bot for the Transsion Core Federation (TCF) according to the following exhaustive specification. The bot manages federation membership, centralized ban/unban with proof uploads, admin hierarchy, appeal system, broadcast, sync, extensive logging, a start menu, and a help system. All code must be written in Python 3.11+ using the python-telegram-bot library version 22.5 (asynchronous, Application.builder()). Use MongoDB as the database with the motor async driver. The bot must be runnable via python -m tgbot_tcf and include a Flask keep-alive server on port 8080. No emoji is allowed in any message, caption, or button. All formatted text must use HTML parse mode.

CREDENTIALS (provided as environment variables; values given here for reference):
- BOT_TOKEN = 8704305954:AAGN9Dd1fTpzQnjvDPMGZa86p0jkXCSykjc
- MONGODB_URI = mongodb+srv://TCF:TCF@clusterfed.c7vnwkg.mongodb.net/?appName=ClusterFed
- Database name: tcf_bot

HARDCODED TELEGRAM IDs (use exactly):
- LOG_CHANNEL = -1003941141635   (all federation events are logged here)
- MAIN_GROUP = -1003872207988    (forum group containing proof and appeal topics)
- PROOF_TOPIC = 67              (thread inside MAIN_GROUP for ban proof uploads)
- APPEAL_TOPIC = 12             (thread inside MAIN_GROUP for appeal submissions)
- APPEAL_DISCUSSION_TOPIC = 11  (thread inside MAIN_GROUP where admins review and decide on appeals)
- MAIN_CHANNEL = -1003852970764 (reference only, not used)
- EXEC_GROUP = -1002333013065   (reference only, not used)
- INITIAL_OWNER_ID = 7146954165 (user ID that becomes the first Federation Owner if fed_owners is empty)

PROJECT STRUCTURE
The code must be organized into a clean, modular structure. The entry point is tgbot_tcf/__main__.py, with sub-packages for database, handlers, utilities, etc. You are free to name the packages, but the structure must be logical and production-ready.

DATABASE SCHEMA (MongoDB collections)
- federated_groups: { chat_id: int, title: str, added_by: int (user_id), added_date: datetime, is_active: bool }
- fed_owners: { user_id: int }  (exactly one document after the first owner is set)
- fed_admins: { user_id: int, promoted_by: int, promoted_date: datetime }
- bans: { ban_id: str (unique, e.g., "{banned_user_id}_{timestamp}"), banned_user_id: int, reason: str, admin_user_id: int, proof_message_id: int, log_message_id: int, previous_proof_message_id: int | null, previous_log_message_id: int | null, timestamp: datetime, updated_timestamp: datetime | null, is_active: bool, update_count: int }

TARGET RESOLUTION (used by every command that takes a target)
1. If the command is a reply, target = replied-to user.
2. If the first argument is a numeric string, treat it as a user ID and call get_chat.
3. Otherwise, treat it as a username (strip @ if present) and call get_chat.
4. If no target can be resolved, reply "Cannot resolve user."

AUTHORIZATION
Commands /cban, /cunban, /cpromote, /cdemote, /transferowner, /broadcast, /rmfed, /syncban, /joinfed, /leaveall, /cleanup require the sender to be in fed_owners or fed_admins. Otherwise reply "You are not authorized."
For /cban and /cunban, authorization is checked purely against the fed_owners/fed_admins collections. The fed admin/owner **does not need to be an admin of the chat** where the command is executed. These commands work in any affiliated group, in the MAIN_GROUP (or its topics), in the EXEC_GROUP, or in bot PM.

BRANDING CONSTANT
Every log message posted to LOG_CHANNEL must contain the exact line:
𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯

DATE/TIME FORMAT
All timestamps displayed or logged must be in UTC and formatted as DD-MM-YYYY | HH:MM (24-hour).

MINIMUM 3 ALIASES FOR EACH COMMAND
Provide at least three aliases for every command that has a target, except where noted. The following list defines the aliases; the bot must recognize all of them and behave identically.

Ban: /cban, /comban, /fban
Unban: /cunban, /comunban, /funban
Promote: /cpromote, /compromote, /fpromote
Demote: /cdemote, /comdemote, /fdemote
Transfer Owner: /transferowner, /tfowner, /fedowner
Broadcast: /broadcast, /announce, /fcast
Sync Ban: /syncban, /forcesync, /fbanall
Remove Federation Group (by ID): /rmfed, /removefed, /deletefed
Disaffiliate Current Group: /defed, /leavefed, /unfed
Check Me: /checkme, /myban, /amibanned
Ban Info: /baninfo, /checkban, /banstatus
List Federated Groups: /fedgroups, /groups, /listfed
Statistics: /fedstats, /stats, /fedinfo
Help: /help, /commands, /start (without deep link; if /start has no argument, show help)
Join Federation (explicit): /joinfed, /requestjoin, /applyfed
Leave All Groups: /leaveall, /exitall, /fedleave
Cleanup Dead Groups: /cleanup, /purge, /fedclean
Federation Information: /about, /tcfabout, /fedabout
Federation Channels: /fedchannels, /channels, /fedconfig

FEATURE 1: GROUP AFFILIATION ON BOT ADD
1. When the bot is added to a group (new_chat_members includes bot, chat.type is group/supergroup), send:
   "Do you want this community to join the Transsion Core Federation?"
   with inline buttons:
   - "Join Federation" -> callback_data = "fed_join"
   - "Cancel" -> callback_data = "fed_cancel"
2. Only the group owner (status "creator") may interact. On any callback:
   - Retrieve chat_member for the clicking user.
   - If status != "creator", answer callback with alert "Only the group owner can decide." (show_alert=True). Do nothing else.
3. Owner clicks "Join Federation":
   a. Check bot's own permissions in the chat via get_chat_member(chat_id, bot_id). Required:
      - can_delete_messages = True
      - can_restrict_members = True
      - can_invite_users = True
   b. If any missing, edit original message to:
      "Please make the bot an admin with the necessary permissions (delete messages, ban users, invite users) and try again." Remove keyboard. Stop.
   c. If permissions are sufficient:
      - If chat already in federated_groups with is_active=true, edit message to "Already affiliated." Remove keyboard. Stop.
      - Otherwise, upsert federated_groups with { chat_id, title, added_by: owner_id, added_date: utcnow(), is_active: true }.
      - If fed_owners is empty, insert { user_id: INITIAL_OWNER_ID } as the first Federation Owner (ignore the group owner for this initial setup).
      - Edit message to "This community is now affiliated with TCF. Federation commands can now be used here by authorized federation admins." Remove keyboard.
      - Log to LOG_CHANNEL (HTML, no buttons):

        Log Message:
        New Affiliated Group
        𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
        Group: <a href="tg://user?id=<chat_id>"><chat_title></a> (ID: <chat_id>)
        Added by Owner: <a href="tg://user?id=<owner_id>">Owner First Name</a> (ID: <owner_id>)
        Date: <DD-MM-YYYY> | <HH:MM>

4. Owner clicks "Cancel":
   a. Edit message to "Affiliation cancelled. Leaving the group." Remove keyboard.
   b. Leave the group using context.bot.leave_chat(chat_id). Handle exceptions silently.
   c. Log to LOG_CHANNEL:

      Log Message:
      Affiliation Rejected & Left
      𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
      Group: <chat_title> (ID: <chat_id>)
      Rejected by Owner: <a href="tg://user?id=<owner_id>">Owner First Name</a> (ID: <owner_id>)
      Date: <DD-MM-YYYY> | <HH:MM>

FEATURE 2: EXPLICIT JOIN FEDERATION LATER (/joinfed, /requestjoin, /applyfed)
- If a group previously rejected or did not join when the bot was added (or the bot was added without the owner present), the group owner can later use /joinfed in the group where the bot is already an admin.
- The bot will re-check its permissions (same as Feature 1 step 3a). If permissions missing, reply "Please make the bot an admin with the necessary permissions (delete messages, ban users, invite users) and try again."
- If permissions OK, perform the same upsert into federated_groups (if already active, reply "Already affiliated."). Set is_active=true. If fed_owners empty, insert { user_id: INITIAL_OWNER_ID }.
- Reply "This community is now affiliated with TCF."
- Log same as New Affiliated Group (Feature 1).

FEATURE 3: DISAFFILIATE A GROUP (multiple aliases)
- /defed, /leavefed, /unfed (inside a federated group):
  - If sender is group owner OR fed owner/admin:
    - Set is_active = false for chat in federated_groups.
    - Reply "This group has been removed from the TCF federation."
    - Try context.bot.leave_chat(chat_id).
    - Log:

      Log Message:
      Group Disaffiliated
      𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
      Group: <chat_title> (ID: <chat_id>)
      Removed by: <a href="tg://user?id=<user_id>">User First Name</a> (ID: <user_id>)
      Date: <DD-MM-YYYY> | <HH:MM>

  - Otherwise reply "Only the group owner or federation admins can disaffiliate this group."

- /rmfed, /removefed, /deletefed <group_id> (any chat, only fed owner/admins):
  - Find active group by chat_id. If not found, reply "Group not found or already removed."
  - Set is_active=false, try to leave, reply "Group <group_id> has been removed from the federation."
  - Log similar to above (same Log Message format).

FEATURE 4: FEDERATION OWNER AND ADMIN MANAGEMENT
- /cpromote, /compromote, /fpromote <target> (Owner only):
  - If no target, reply "Reply to a user, provide a user ID, or provide a username to promote."
  - Resolve target. Add to fed_admins (if already there, reply "Already a Federation Admin.").
  - Reply "User <user_id> is now a Federation Admin."
  - Log:

    Log Message:
    New Federation Admin Promoted
    𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
    Admin: <a href="tg://user?id=<target_id>">Target First Name</a> (ID: <target_id>)
    Promoted by Owner: <a href="tg://user?id=<owner_id>">Owner First Name</a> (ID: <owner_id>)
    Date: <DD-MM-YYYY> | <HH:MM>

- /cdemote, /comdemote, /fdemote <target> (Owner only):
  - Remove from fed_admins. Reply "User demoted from Federation Admin." or "Not a Federation Admin."
  - Log:

    Log Message:
    Federation Admin Demoted
    𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
    Admin: <a href="tg://user?id=<target_id>">Target First Name</a> (ID: <target_id>)
    Demoted by Owner: <a href="tg://user?id=<owner_id>">Owner First Name</a> (ID: <owner_id>)
    Date: <DD-MM-YYYY> | <HH:MM>

- /transferowner, /tfowner, /fedowner <target> (Owner only):
  - Move ownership: update fed_owners, old owner becomes a regular admin (fed_admins entry if not already there).
  - Reply "Ownership transferred to <user_id>."
  - Log:

    Log Message:
    Federation Ownership Transferred
    𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
    New Owner: <a href="tg://user?id=<target_id>">Target First Name</a> (ID: <target_id>)
    Previous Owner: <a href="tg://user?id=<old_owner_id>">Old Owner First Name</a> (ID: <old_owner_id>)
    Date: <DD-MM-YYYY> | <HH:MM>

FEATURE 5: COMMUNITY BAN (/cban, /comban, /fban) WITH PROOF (with Cancel button)
Only fed owner/admins. Usage: /cban <target> <reason> (reason required). Works in any affiliated group, in the MAIN_GROUP (forum), in the EXEC_GROUP, or in bot PM. The executor does not need to be an admin of the chat.
Implement with ConversationHandler (60-second timeout per step).

1. Entry: Bot replies to command with a message containing:
   - Text: "Please provide proof for this ban. Send a photo or video (multiple media allowed). You have 60 seconds."
   - Inline keyboard with one button: "Cancel" (callback_data="cancel_proof")
   Store sent message ID for later editing.

2. State WAITING_PROOF:
   - If the user clicks the "Cancel" button (callback_data="cancel_proof"):
     - Edit the message to "Operation cancelled." and remove keyboard.
     - End the conversation.
   - Accept photo, video, or media_group_id (album).
     - For media groups: collect messages by media_group_id. Set a 2-second asyncio task (or JobQueue) that fires when no new media for that group arrives; process all accumulated media as one album.
   - Any other message type -> reply "Only photos and videos allowed." and stay in state.
   - Timeout (60s): edit the prompt message to "Proof submission timed out." and remove keyboard, then exit conversation.
   with inline buttons in one row:
     - "Join Federation" | "Cancel"

3. When media collected:
   - Determine if target already actively banned (is_active=true). If yes -> UPDATE, else NEW.
   - Generate unique ban_id = f"{target.id}_{int(datetime.utcnow().timestamp())}".
   - Upload media to MAIN_GROUP, message_thread_id=PROOF_TOPIC (67):
     * Single: send_photo/send_video with caption according to templates below.
     * Album: send_media_group with InputMediaPhoto/InputMediaVideo; first item gets caption, rest caption=None.
   - Capture first message_id as proof_message_id. Construct proof link: https://t.me/c/3872207988/<proof_message_id>?thread=67
   - Send log message to LOG_CHANNEL (no thread) with HTML and inline keyboard (see templates). Capture log_message_id.
   - Save/update ban record in bans collection.
   - Edit the initial prompt message to: "User <banned_user_id> has been banned from the federation. Reason: <reason>" and remove the Cancel button.
   - End conversation.

Templates for NEW BAN (in MAIN_GROUP proof topic):

Caption Message (Proof Topic):
ID: <banned_user_id>
Fed Admin: <a href="tg://user?id=<admin_id>">Admin's First Name</a>
Fed Admin ID: <admin_id>

Commit at: <DD-MM-YYYY> | <HH:MM>

Log Message (LOG_CHANNEL):
New Community Ban
𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
Admin: <a href="tg://user?id=<admin_id>">Admin First Name</a>
User: <a href="tg://user?id=<banned_user_id>">User First Name</a>
User ID: <banned_user_id>
Reason: <reason>

Commit at: <DD-MM-YYYY> | <HH:MM>
+ Inline keyboard button: "Proof <banned_user_id>" url = proof link.

Templates for UPDATE BAN (when user already banned):

Caption Message (Proof Topic):
ID: <banned_user_id>
Fed Admin: <a href="tg://user?id=<admin_id>">Admin's First Name</a>
Fed Admin ID: <admin_id>
Previous: <a href="<previous_proof_link>">Click Here</a>

Commit at: <original ban date> | <original time>
Update at: <DD-MM-YYYY> | <HH:MM>

Log Message (LOG_CHANNEL):
New Community Ban (Update)
𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
Admin: <a href="tg://user?id=<new_admin_id>">New Admin</a>
Previous Admin: <a href="tg://user?id=<old_admin_id>">Old Admin</a>
User: <a href="tg://user?id=<banned_user_id>">User</a>
User ID: <banned_user_id>
Reason: <new_reason>
Previous: <a href="<previous_log_link>">Click Here</a>

Commit at: <original ban date> | <original time>
Update at: <DD-MM-YYYY> | <HH:MM>
+ Inline keyboard with two buttons:
  1. "Proof <banned_user_id>" -> new proof link.
  2. "Previous Proof <banned_user_id>" -> previous proof link.

BAN RECORD UPDATE LOGIC:
On update: set previous_proof_message_id = old proof_message_id, previous_log_message_id = old log_message_id, then overwrite with new IDs. Increment update_count, set updated_timestamp. Keep original timestamp and is_active unchanged.

FEATURE 6: COMMUNITY UNBAN (/cunban, /comunban, /funban)
- Only fed owner/admins. Usage: /cunban <target>. Works in any affiliated group, in the MAIN_GROUP (forum), in the EXEC_GROUP, or in bot PM. The executor does not need to be an admin of the chat.
- Find active ban by banned_user_id. If none, reply "User is not banned."
- Unban also occurs automatically when an appeal is approved (see Feature 8).
- Set is_active = false.
- Log to LOG_CHANNEL:

  Log Message:
  Community Unban
  𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
  Admin: <a href="tg://user?id=<admin_id>">Admin First Name</a>
  User: <a href="tg://user?id=<target_id>">User First Name</a>
  User ID: <target_id>
  Date: <DD-MM-YYYY> | <HH:MM>

- Reply to issuer: "User <target_id> has been unbanned from the federation."

FEATURE 7: CHECK BAN STATUS (/checkme, /myban, /amibanned)
- Any user, any chat. Query bans for sender.id, is_active=true.
- If banned:
  You are currently banned from TCF.
  Reason: <reason>
  Banned by Federation Admin: <admin first name>
  + inline button: "Submit Appeal" url = https://t.me/<bot_username>?start=appeal_<ban_id>
- If not banned: "You are not banned in this federation."

FEATURE 8: APPEAL SYSTEM (with Cancel button and Admin Review)
- Deep link: /start appeal_<ban_id> (private chat only).
1. Find ban by ban_id. If not found or not active, reply "Invalid or expired ban."
2. Send the exactly specified appeal instruction text (the text starting with "Transsion Core [Group]:" and ending with "Log Channel: @TranssionCoreFederationLogs"). No emoji. At the bottom of this message, add an inline button "Cancel" with callback_data="cancel_appeal".
3. Enter state WAITING_APPEAL. Accept only messages starting with "#appeal". All other messages are ignored silently.
4. If the user clicks "Cancel":
   - Edit the instruction message to "Appeal cancelled." and remove the keyboard.
   - End the conversation.
5. When a #appeal message received:
   - Parse lines: Log link, Clarification, Agreement (each on a new line with labels).
   - Validate that the log link contains the stored log_message_id. If mismatch, reply "Invalid log link. Please check and try again." and stay in state (keep the Cancel button on the instruction message).
   - If valid:
     a. Post the entire raw appeal message to MAIN_GROUP, message_thread_id=APPEAL_TOPIC (12). This is a plain text message; no extra formatting. Capture its message_id as appeal_message_id.
     b. Construct appeal link: https://t.me/c/3872207988/<appeal_message_id>?thread=12
     c. **Send an appeal review message to MAIN_GROUP, message_thread_id=APPEAL_DISCUSSION_TOPIC (11) with the following format (HTML) and inline keyboard:**
        ```
        New Appeal Request
        User: <a href="tg://user?id=<user_id>">User First Name</a> (ID: <user_id>)
        Ban ID: <ban_id>
        Appeal: <appeal_link>
        Submitted: <DD-MM-YYYY> | <HH:MM>

        This appeal is pending review. 
        ```
        Inline keyboard:
        - Button "Approve" -> callback_data="appeal_approve_<ban_id>"
        - Button "Reject" -> callback_data="appeal_reject_<ban_id>"
        Store the message_id of this review message as review_message_id (in a temporary dict or database, keyed by ban_id). Also record the timestamp of appeal submission (now_utc) and the admin who originally banned (ban.admin_user_id).
     d. Edit the instruction message to "Your appeal has been submitted. Federation admins will review it." and remove keyboard.
     e. Log to LOG_CHANNEL:

        Log Message:
        New Appeal Submitted
        𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
        User: <a href="tg://user?id=<user_id>">User First Name</a> (ID: <user_id>)
        Ban ID: <ban_id>
        Appeal: <appeal_link>
        Date: <DD-MM-YYYY> | <HH:MM>

     f. End conversation.

6. **Appeal Review Callback Handling:**
   - When an admin clicks "Approve" or "Reject" on the review message (thread 11):
     - Verify the clicking user is a fed owner or fed admin.
     - If not authorized, answer callback with alert "You are not authorized." and do nothing.
     - Retrieve the ban record by ban_id. If ban is no longer active, answer callback "This ban is already inactive." and edit the review message to "Appeal resolved (ban no longer active)." remove keyboard.
     - Determine the time elapsed since the appeal submission (review message timestamp). If elapsed time is less than 12 hours, only the original banning admin (ban.admin_user_id) may click the buttons. If the clicking user is not the original banning admin, answer callback with alert "Only the banning admin can review within the first 12 hours." and do nothing.
     - If elapsed time >= 12 hours, any fed admin/owner may click.
     - Process decision:
       * If "Approve":
         - Set ban.is_active = false (unban) and update the ban record.
         - Edit the review message to "Appeal approved by <a href="tg://user?id=<admin_id>">Admin First Name</a>. User has been unbanned." and remove keyboard.
         - Log to LOG_CHANNEL:
           Log Message:
           Appeal Approved
           𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
           User: <a href="tg://user?id=<user_id>">User First Name</a> (ID: <user_id>)
           Ban ID: <ban_id>
           Approved by: <a href="tg://user?id=<admin_id>">Admin First Name</a>
           Date: <DD-MM-YYYY> | <HH:MM>
         - Optionally notify the user in PM that their appeal was approved and they are now unbanned.
       * If "Reject":
         - Edit the review message to "Appeal rejected by <a href="tg://user?id=<admin_id>">Admin First Name</a>." and remove keyboard.
         - Log to LOG_CHANNEL:
           Log Message:
           Appeal Rejected
           𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
           User: <a href="tg://user?id=<user_id>">User First Name</a> (ID: <user_id>)
           Ban ID: <ban_id>
           Rejected by: <a href="tg://user?id=<admin_id>">Admin First Name</a>
           Date: <DD-MM-YYYY> | <HH:MM>
         - Optionally notify the user in PM that their appeal was rejected.
   - All callback queries must be answered with an appropriate alert (or no alert if the edit is done, but ensure the callback is acknowledged to stop the loading spinner).
   - If the ban becomes inactive before review (e.g., manually unbanned), the review message should be edited to "Appeal resolved (user already unbanned)." and keyboard removed. This check should be done on each callback as well.
   Inline keyboard (single row):
- "Approve" | "Reject"

FEATURE 9: BAN DETAILS (/baninfo, /checkban, /banstatus)
- /baninfo <target> – available to everyone.
- If not banned, reply "User is not banned in the federation."
- If banned, reply (HTML):
  Ban Details
  User: <a href="tg://user?id=<banned_user_id>">User First Name</a>
  User ID: <banned_user_id>
  Reason: <reason>
  Banned by: <a href="tg://user?id=<admin_id>">Admin First Name</a>
  Date: <DD-MM-YYYY> | <HH:MM>
  Ban ID: <ban_id>
  Status: Active
  (If update_count > 0, add "Last Updated: <updated_timestamp>")
  + inline button "View Proof" url = current proof link.

FEATURE 10: LIST FEDERATED GROUPS (/fedgroups, /groups, /listfed)
- /fedgroups – everyone.
- Fetch all active federated_groups.
- If none: "No groups are currently affiliated with TCF."
- Else list up to 50 as plain text: "<title> (ID: <chat_id>)". If more, add "... and X more groups."

FEATURE 11: FEDERATION STATISTICS (/fedstats, /stats, /fedinfo)
- /fedstats – everyone.
- Reply (HTML):
  TCF Federation Statistics
  Owner: <a href="tg://user?id=<owner_id>">Owner First Name</a>
  Federation Admins: <count>
  Affiliated Groups: <count>
  Active Bans: <count>

FEATURE 12: BROADCAST (/broadcast, /announce, /fcast)
- /broadcast <message> – fed owner/admins. If no text, reply "Please provide a message to broadcast."
- Iterate all active federated groups, send plain text message. Catch exceptions, mark failed groups as is_active=false.
- Reply: "Broadcast sent to <success> groups. Failed: <failure> groups."
- Log:

  Log Message:
  Broadcast Sent
  𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
  Admin: <a href="tg://user?id=<admin_id>">Admin First Name</a>
  Message: <first 100 chars>
  Groups reached: <success>
  Failed groups: <failure>
  Date: <DD-MM-YYYY> | <HH:MM>

FEATURE 13: FORCE BAN SYNC (/syncban, /forcesync, /fbanall)
- /syncban <target> – fed owner/admins.
- Find active ban. If not, reply "User is not banned in the federation."
- For each active federated group where bot has can_restrict_members, attempt ban_chat_member(chat_id, target_id). Count successes and failures (catch exceptions).
- Reply: "Ban enforced across <success> groups. Failed: <failure> groups."
- Log:

  Log Message:
  Ban Synced Across Groups
  𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
  Admin: <a href="tg://user?id=<admin_id>">Admin First Name</a>
  User: <a href="tg://user?id=<target_id>">Target First Name</a> (ID: <target_id>)
  Groups enforced: <success>
  Failures: <failure>
  Date: <DD-MM-YYYY> | <HH:MM>

FEATURE 14: BOT REMOVED FROM GROUP
- On my_chat_member update (new status kicked/left). If chat was active in federated_groups, set is_active=false. Log:

  Log Message:
  Group Removed Bot
  𝘛𝘊𝘍 - 𝘛𝘳𝘢𝘯𝘴𝘴𝘪𝘰𝘯 𝘊𝘰𝘳𝘦 𝘍𝘦𝘥𝘦𝘳𝘢𝘵𝘪𝘰𝘯
  Group: <chat_title> (ID: <chat_id>)
  Date: <DD-MM-YYYY> | <HH:MM>

FEATURE 15: LEAVE ALL GROUPS (/leaveall, /exitall, /fedleave)
- Only fed owner. The bot leaves every active federated group (is_active=true). For each, attempt leave_chat. Set each group's is_active=false after successfully leaving. Count successes and failures.
- Reply: "Left <success> groups. Failed to leave <failure> groups."
- Log each departure individually as "Group Disaffiliated" with admin set as the owner who issued the command.

FEATURE 16: CLEANUP DEAD GROUPS (/cleanup, /purge, /fedclean)
- Only fed owner/admins. Iterate all federated_groups with is_active=true. For each, check if bot is still a member (use get_chat or get_chat_member). If bot is no longer a member (exception or status "left"/"kicked"), set is_active=false and log the removal as "Group Disaffiliated" with admin = owner/admin who initiated cleanup. Count cleaned groups. Reply: "Cleaned up <count> groups that were no longer accessible."

FEATURE 17: FEDERATION INFORMATION (/about, /tcfabout, /fedabout)
- Sends a static informational message about TCF as provided by the user:
  "What's is TCF?
  Transsion Core Federation (TCF) is a community-driven federation for Infinix, Tecno, and Itel groups. Our main focus is maintaining group security and a conducive environment so members can discuss comfortably.

  TCF is not an official part of Transsion Holdings. This is strictly an independent community.

  History
  Established in 2024. Originally named TFI, but it was disbanded due to internal issues. Shortly after, TCF was formed to continue managing the community with better stability."

  Include no buttons, just the text. This command can be used by anyone.

FEATURE 18: FEDERATION CHANNELS (/fedchannels, /channels, /fedconfig)
- Displays the hardcoded channel/group IDs used by the bot. Format:
  Log Channel: -1003941141635
  Main Group: -1003872207988
  Proof Topic: 67
  Appeal Topic: 12
  Main Channel: -1003852970764
  Exec Group: -1002333013065

FEATURE 19: HELP COMMAND (/help, /commands, /start without deep link)
- Shows a list of all available commands with their aliases and a brief description of each, without emoji. This is the plain command-based help, separate from the interactive help system described in Feature 25.

FEATURE 20: MESSAGE EDITING BEHAVIOUR
- Prefer editing existing bot messages instead of sending new ones, unless a new message is explicitly required. Use try-except to ignore "Message is not modified" errors. In conversation flows that use Cancel buttons, edit the same message to indicate cancellation or completion.

FEATURE 21: ERROR HANDLING AND LOGGING
- Use Python logging module (INFO). All handlers must catch exceptions to avoid crashes; log full tracebacks.

FEATURE 22: KEEP-ALIVE SERVER
- Minimal Flask app listening on 0.0.0.0:8080, returns "OK" on /. Start in daemon thread before run_polling().

FEATURE 23: BOT STARTUP
- In __main__.py: load env vars (BOT_TOKEN, MONGODB_URI), connect MongoDB, build Application with all handlers (command handlers for each alias, conversation handlers, callback handlers, chat member update handler, error handler), start keep-alive thread, run_polling(allowed_updates=Update.ALL_TYPES).

FEATURE 24: START MESSAGE & MAIN MENU
- When a user sends /start in private chat (no deep link), the bot sends a welcome message summarizing TCF briefly (e.g., "Welcome to Transsion Core Federation (TCF) Bot...") and displays an inline keyboard with the following options:
  - Inline keyboard arranged as:
    Row 1: "About TCF", "Help"
    Row 2: "Statistics", "Federation Groups"
    Row 3: "Federation Info"
  - "About TCF" -> callback_data="menu_about"
  - "Help" -> callback_data="menu_help"
  - "Statistics" -> callback_data="menu_stats"
  - "Federation Groups" -> callback_data="menu_groups"
  - "Federation Info" -> callback_data="menu_fedinfo"
- Clicking each button triggers an edit of the same message:
  - "About TCF": display the same static text used in Feature 17 (the long description). At the bottom, include a "Back" button with callback_data="menu_back_start" that returns to the main start menu (re-displaying the welcome text and original buttons).
  - "Help": opens the interactive help system described in Feature 25. When accessed from here, all submenus must have a "Back" button that returns to the previous help level, and the top-level help menu must include a "Back" button (callback_data="menu_back_start") to return to the main start menu.
  - "Statistics": display the same statistics as /fedstats (Feature 11), with a "Back" button to the start menu.
  - "Federation Groups": display the list of active federated groups as in /fedgroups (Feature 10), with a "Back" button.
  - "Federation Info": display the channel/group IDs as in /fedchannels (Feature 18), with a "Back" button.
- All navigation must be done by editing the existing message, not sending new ones. Only the user who issued /start can interact with these buttons (check callback.from_user.id against original user).

FEATURE 25: INTERACTIVE HELP SYSTEM
- The help system is accessible both via /help (Feature 19, plain command) and via the "Help" button in the start menu (Feature 24). When accessed from the start menu, a "Back" button must be present at every level.
- When the user opens the interactive help (via menu_help callback or any entry point that triggers the interactive version), the bot edits the message to show a list of available feature modules as inline buttons. Example list (adjust based on your final features):
  - "Ban /cban" -> callback_data="help_ban"
  - "Unban /cunban" -> callback_data="help_unban"
  - "Check Ban" -> callback_data="help_check"
  - "Promote / Demote" -> callback_data="help_admin"
  - "Transfer Owner" -> callback_data="help_transfer"
  - "Broadcast" -> callback_data="help_broadcast"
  - "Sync Ban" -> callback_data="help_syncban"
  - "Disaffiliate" -> callback_data="help_defed"
  - "Appeal" -> callback_data="help_appeal"
  - (Add more as needed for your feature set)
  - At the bottom, place the "Back" button (callback_data="menu_back_start") if opened from the start menu; if opened from /help command, no "Back" button is needed.
- When a user clicks a feature button, the bot edits the message to show detailed instructions about that feature:
  - Full syntax (including all aliases, required vs optional arguments, reply/id/username support).
  - Who can use it (owner, admin, anyone).
  - Where it works (affiliated groups, PM, specific topics).
  - A brief example of usage.
  - No internal IDs or sensitive information.
- At the bottom of each detailed page, provide a "Back" button (callback_data="menu_help") to return to the module list.
- If the interactive help was entered from /help (command), no "Back" buttons should be present in the module list or detail pages; the user can simply type /help again if needed.
- Ensure only the original user can navigate the interactive help by checking callback.from_user.id.

FEATURE 26: INLINE KEYBOARD LAYOUT RULES
- All inline keyboards must be arranged efficiently to keep messages compact.
- General rules:
  * For menus with multiple options (e.g., main menu, help module list), use **2 buttons per row**.
  * For confirmation actions (e.g., Approve/Reject, Join/Cancel), place the two related buttons **in the same row**.
  * A single button (e.g., "Back") may occupy one row by itself.
  * Never use one button per row for lists longer than 2 items; group them into rows of 2.
- Apply these layouts:
  * FEATURE 24 (Main Menu): 5 buttons → arrange as 2 rows of 2 buttons each, then 1 row with the remaining 1 button (2-2-1).
  * FEATURE 25 (Help Module List): use 2 buttons per row for all feature entries.
  * FEATURE 8 (Appeal Review): "Approve" and "Reject" buttons must be placed in the same row.
  * FEATURE 1 & FEATURE 2 (Join/Cancel): "Join Federation" and "Cancel" buttons in the same row.
  * FEATURE 5 (Cancel Proof): single "Cancel" button can be alone in one row, but if additional actions exist, group them appropriately.
- The goal is to reduce message height and improve readability without using emoji.

DELIVERABLE
Produce the full source code implementing all 25 features exactly as described, including all alias commands (minimum 3 per command) and the exact Caption/Log message templates specified. The code must be production-ready and require only setting environment variables. No emoji anywhere. All log messages must include the exact federation branding line as shown.